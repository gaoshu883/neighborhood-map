// 全局变量
// var app = app || {};

// 最后绑定数据
// ko.applyBindings(app.viewModel);


// window.onload = function() {
//   // 为了便于调试程序，将实例存储在app中
//   // 后期应该将其去除
//   app.viewModel = new app.ViewModel();
//   ko.applyBindings(app.viewModel);
// };